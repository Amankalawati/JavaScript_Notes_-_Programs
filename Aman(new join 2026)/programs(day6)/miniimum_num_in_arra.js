let arr = [5,10,40,20,100,38,6];
let min1 = arr[0];
let min2 = arr[1];

/*for(let i=0; i<= arr.length;i++)
{
    if( arr[i]<min)
    {
        min = arr[i];
    }
}
console.log("minimum value in array: "+min);


 let smin = 9999;
 for(i=0; i<=arr.length;i++)
 {
    if(arr[i] != min && arr[i]<smin)
    {
        smin = arr[i];
    }
 }
 console.log("second lowest number in array: "+smin);

 
 let tmin = 99999;
 for(i=0; i<=arr.length;i++)
 {
    if(arr[i] != min && arr[i] != smin && arr[i]<tmin)
    {
        tmin= arr[i];
    }
 }
 console.log("third minimum: "+tmin);**/
